# Versiona Configuration Example
# ===============================
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Database (PostgreSQL)
# Install: brew install postgresql@16 && brew services start postgresql@16
# Create DB: createdb versiona
DATABASE_URL=postgresql://localhost:5432/versiona
